<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.bbb.model.dao.VirtualBankDao">

	<select id="findById">
		SELECT bank_id, bank_name, webhook_url
		FROM virtual_banks
		WHERE bank_id = #{bankId}
	</select>

	<select id="findAll">
		SELECT bank_id, bank_name, webhook_url
		FROM virtual_banks
		ORDER BY bank_id ASC
	</select>

</mapper>